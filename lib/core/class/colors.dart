import 'package:flutter/material.dart';

class AppColors {
  static const Color pri = Colors.cyan;
  static const Color thi = Colors.cyanAccent;
  static const Color sec = Colors.lightBlueAccent;
  static const Color white = Color(0xffffffff);
  static const Color black = Color.fromARGB(255, 0, 0, 0);
  static const Color red = Color.fromARGB(255, 236, 66, 66);
}

class AppColor {
  static const Color pri = Color(0xFF00BCD4); // أزرق فاتح
  static const Color sec = Color(0xFF0097A7); // أزرق داكن
  static const Color background = Color(0xFFF5F5F5); // خلفية فاتحة
  static const Color title = Color(0xFF212121); // للنصوص الرئيسية
  static const Color subtitle = Color(0xFF757575); // للنصوص الثانوية
}
